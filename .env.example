# Copy to .env for local development; set the same keys in Vercel →
# Project → Settings → Environment Variables for production.

# Supabase → Project Settings → API
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxx

# Optional: override the WhatsApp number that receives orders
# (defaults to the shop's +91 89760 35333 in src/content.js)
# VITE_WHATSAPP_NUMBER=918976035333
